{
	"name": "wobi",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"prepare": "svelte-kit sync || echo ''",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"db:push": "bun --env-file=.env drizzle-kit push --force",
		"db:generate": "bun --env-file=.env drizzle-kit generate",
		"db:migrate": "bun --env-file=.env drizzle-kit migrate",
		"db:studio": "bun --env-file=.env drizzle-kit studio",
		"db:import-players": "bun --env-file=.env run scripts/import-players.ts",
		"db:import-fixtures": "bun --env-file=.env run scripts/import-fixtures.ts",
		"db:export-players": "bun --env-file=.env run scripts/export-players.ts",
		"db:dump": "bun --env-file=.env run scripts/dump-db.ts"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^7.0.1",
		"@sveltejs/kit": "^2.63.0",
		"@sveltejs/vite-plugin-svelte": "^7.1.2",
		"@tailwindcss/vite": "^4.3.0",
		"@types/node": "^26",
		"drizzle-kit": "^0.31.10",
		"drizzle-orm": "^0.45.2",
		"postgres": "^3.4.9",
		"svelte": "^5.56.1",
		"svelte-check": "^4.6.0",
		"tailwindcss": "^4.3.0",
		"typescript": "^6.0.3",
		"vite": "^8.0.16"
	}
}
