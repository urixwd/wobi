# WOBI — תיעוד מוצרי

WOBI הוא כלי עזר ל**ליגת החלומות (Dream Team / Sport5)**: לבחור חילופים והרכבים עם נתונים מקומיים (מחיר, נקודות, לוח משחקים, תמורה לכסף).

## נכסים גרפיים
ב־UI משתמשים **רק בלוגואים של קבוצות** (מגרש, טבלה, רמזור, לוח משחקים) + פונט Noto Sans Hebrew מקומי.
אין תמונות ראש של שחקנים ואין חולצות — לא מוצגים ולא נשמרים מקומית.
הלוגואים ב־`static/assets/sport5/`.

## מקור נתוני שחקנים (חשוב)

### מה קרה ל־`players.json`?
הקובץ **נמחק מהריפו**. הוא היה dump חד־פעמי של Sport5 לפני מחזור 4. מעכשיו:
- האפליקציה קוראת שחקנים **רק מה־DB**
- dump JSON של Sport5 הוא **קלט זמני** לייבוא מחזור (`--gw=N`), לא קובץ קבוע בפרויקט
- גיבוי שמתחייב לגיט הוא **`db/dumps/latest.sql`** (pg_dump של כל ה־WOBI), לא JSON של שחקנים

- **מקור האמת הוא PostgreSQL**, לא קובץ בריפו.
- כל מחזור מגיע כ־dump מ־Sport5 → מיובא ל־DB עם מספר מחזור (`--gw=N`).
- ב־DB נשמר **snapshot לכל מחזור** (`player_snapshots`) + טבלת `players` כתצוגה חיה של המחזור האחרון שיובא.
- כרגע במערכת: **מחזור 4** בלבד.
- **זרימה עתידית:** בעוד כמה ימים אביא קובץ חדש ואבקש לייבא אותו כ־**מחזור 5**, ואז 6… עד סוף העונה. אין צורך (ואסור) להשאיר `players.json` קבוע בגיט.

## למי זה מיועד
שחקן פנטזי שמנהל קבוצה אחת, מסך עברי צפוף, עד 3 חילופים לשבוע.

## ניווט
| קישור | הערות |
|---|---|
| הקבוצה שלי | ברירת מחדל (`/` → `/squad`) |
| סקיצות | טיוטות לפי מחזור |
| אפשרויות קבוצה | עד 3 חילופים מהשמור |
| מעקב | רשימה קבועה + רשימת מחזור, והצעות חילופים למחזור |

## מסכים

### הקבוצה שלי (`/squad`)
מגרש, ספסל, טבלה, פילטרים (יריבה / לוח 5 / קבוצות / מחיר / עמדות), מגבלות (120מ, מקס׳ 2 לקבוצה), staged מ־options/סקיצות, משחקי מחזור למטה.

**לוח (5):** ממוצע קושי 5 משחקים (ירוק=1, צהוב=2, אדום=3) → קל ≤1.5 · בינוני ≤2.25 · קשה מעל.

### סקיצות / אפשרויות / מעקב
כמפורט קודם: חילופים מול השמור, staged ב־URL, הרכב סופי ב־`final_squads`, תצוגת כרטיסים משותפת (`CompactPlayerCard`). ב־`/watchlist` יש הצעות חילופים למחזור מתוך רשימת המחזור.

## מושגים
| מושג | משמעות |
|---|---|
| תקציב | 120מ ל־15 |
| מקס׳ לקבוצה | 2 |
| vlfm | נק׳ עונה ÷ מחיר |
| עשירון | דירוג לפי נק׳ עונה (בלי 0 נק׳) |
| snapshot מחזור | `player_snapshots` לפי GW |
| staged | טיוטה ב־URL/טאב, לא DB |

## זרימת עדכון מחזור (חוזרת)
ההוראות המלאות — כולל תוצאות המחזור שהסתיים, לא רק dump שחקנים — ב־[docs/NEW-GAMEWEEK.md](NEW-GAMEWEEK.md).

בקצרה, שני דברים נפרדים:

1. dump שחקנים (לא בגיט) → `bun run db:import-players -- --gw=P ./incoming/....json`
2. תוצאות המחזור שהסתיים → לעדכן ציונים ב־`fixtures.json` (הלוח המלא, כן בגיט) ואז `bun run db:import-fixtures`

אחר כך `bun run db:dump` ושני commits: קבצים, ואז `db/dumps/latest.sql`.

## עדיין חלקי
מומנטום מ־GW7 · שילוב watchlist באפשרויות · סריקת Dream Team חיה
